{
  "name": "backend",
  "version": "0.0.1",
  "description": "\"\"",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "build:ui": "rm -rf build && cd ../Full-stack-open-practice && npm run build && xcopy -r build ../FSO-npm-server",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin part3-1"
  },
  "author": "Kushal Tiwari",
  "license": "UNLICENSED",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
